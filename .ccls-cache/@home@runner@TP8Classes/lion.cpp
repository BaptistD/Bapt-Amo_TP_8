#include ""
#include "lion.h"
using namespace std;

void lion::setAttaque() const { return this->? ; }

void lion::deplace(int maxX, int maxY) const {
  int X = [ -1, 1 ];
  int Y = [ -1, 1 ];
}

ostream &operator<<(ostream &s, lion const &cr) {
  s << "attaque =" << cr.setAttaque() << endl;
  return s;
}